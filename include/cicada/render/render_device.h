#ifndef CICADA_RENDER_RENDER_DEVICE_HEADER_GUARD
# define CICADA_RENDER_RENDER_DEVICE_HEADER_GUARD (1)

# include "cicada/macros.h"

# ifdef CICADA_PRAGMA_ONCE
#  pragma once
# endif

namespace ccd {
	class render_device
	{
	public:

	};
}

#endif