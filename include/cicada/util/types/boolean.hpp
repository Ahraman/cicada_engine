#ifndef CICADA_UTIL_TYPES_BOOLEAN_HEADER_GUARD
# define CICADA_UTIL_TYPES_BOOLEAN_HEADER_GUARD (1)

# include "cicada/macros.h"

# ifdef CICADA_PRAGMA_ONCE
#  pragma once
# endif

namespace ccd {
	template<typename B>
	concept boolean = requires {

	};
}

#endif